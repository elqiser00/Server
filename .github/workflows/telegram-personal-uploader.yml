# .github/workflows/telegram-uploader.yml

name: Telegram Movie Uploader

on:
  workflow_dispatch:
    inputs:
      chat_id:
        description: 'معرف القناة/الجروب'
        required: true
        type: string
        default: '1548535280-'
      content_name:
        description: 'اسم الفيلم'
        required: true
        type: string
        default: 'Truth & Treason 2025'
      image_url:
        description: 'رابط صورة الغلاف'
        required: true
        type: string
        default: 'https://img.downet.net/uploads/U8xQf.webp'
      video_url:
        description: 'رابط الفيديو (.mp4 فقط)'
        required: true
        type: string

jobs:
  upload:
    runs-on: ubuntu-latest
    
    steps:
    - name: عرض بيانات الإدخال
      run: |
        echo "🎬 بدء رفع فيلم إلى Telegram"
        echo "========================================"
        echo "📝 اسم الفيلم: ${{ github.event.inputs.content_name }}"
        echo "👥 إلى: ${{ github.event.inputs.chat_id }}"
        echo "🖼️ الصورة: ${{ github.event.inputs.image_url }}"
        echo "🎥 الفيديو: ${{ github.event.inputs.video_url }}"
        echo "⏰ الوقت: $(date)"
        echo "========================================"
        
    - name: تثبيت الأدوات الأساسية
      run: |
        echo "🔧 تثبيت الأدوات..."
        sudo apt-get update -y
        sudo apt-get install -y wget ffmpeg python3 python3-pip
        
        echo "✅ تم تثبيت الأدوات"
        
    - name: تثبيت Telethon
      run: |
        echo "🐍 تثبيت Telethon..."
        pip3 install telethon
        
        echo "✅ تم تثبيت Telethon"
        
    - name: إنشاء سكربت الرفع البسيط
      run: |
        cat > upload.py << 'EOF'
        import asyncio
        import os
        import sys
        import subprocess
        from telethon import TelegramClient
        from telethon.sessions import StringSession
        
        # بيانات من Secrets
        API_ID = int(os.environ['TELEGRAM_API_ID'])
        API_HASH = os.environ['TELEGRAM_API_HASH'])
        SESSION_STRING = os.environ['TELEGRAM_SESSION_STRING'])
        
        # بيانات من GitHub
        CHAT_ID = os.environ['CHAT_ID'])
        CONTENT_NAME = os.environ['CONTENT_NAME'])
        IMAGE_URL = os.environ['IMAGE_URL'])
        VIDEO_URL = os.environ['VIDEO_URL'])
        
        def log(message):
            print(f'[INFO] {message}', flush=True)
        
        def download_file(url, filename):
            """تنزيل ملف باستخدام wget مع خيارات"""
            log(f'تنزيل {filename}...')
            
            # استخدام wget مع خيارات متقدمة
            cmd = [
                'wget',
                '--no-check-certificate',  # تجاوز SSL
                '-c',  # استئناف التحميل
                '-O', filename,
                url
            ]
            
            try:
                result = subprocess.run(cmd, capture_output=True, text=True)
                if result.returncode == 0:
                    log(f'✅ تم تنزيل {filename}')
                    return True
                else:
                    log(f'❌ فشل تنزيل {filename}: {result.stderr}')
                    return False
            except Exception as e:
                log(f'❌ خطأ في التنزيل: {e}')
                return False
        
        async def main():
            log('🚀 بدء عملية الرفع...')
            
            # تنظيف اسم الفيلم
            import re
            clean_name = re.sub(r'[<>:"/\\|?*]', '', CONTENT_NAME).strip()
            clean_name = re.sub(r'\s+', ' ', clean_name)
            
            log(f'📁 الاسم النظيف: {clean_name}')
            
            # إنشاء عميل Telegram
            client = TelegramClient(
                StringSession(SESSION_STRING),
                API_ID,
                API_HASH
            )
            
            try:
                # الاتصال
                await client.connect()
                log('✅ متصل بـ Telegram')
                
                # 1. تنزيل ورفع الصورة
                image_filename = "poster.jpg"
                if download_file(IMAGE_URL, image_filename):
                    log('📤 رفع الصورة...')
                    await client.send_file(
                        CHAT_ID,
                        image_filename,
                        caption=f'🎬 {CONTENT_NAME}'
                    )
                    log('✅ تم رفع الصورة')
                
                # 2. تنزيل ورفع الفيديو
                video_filename = f"{clean_name}.mp4"
                if download_file(VIDEO_URL, video_filename):
                    log('📤 رفع الفيديو...')
                    await client.send_file(
                        CHAT_ID,
                        video_filename,
                        caption=f'🎥 {CONTENT_NAME}\n📁 {video_filename}'
                    )
                    log('✅ تم رفع الفيديو')
                
                log('🎉 تم اكتمال عملية الرفع بنجاح!')
                
            except Exception as e:
                log(f'❌ خطأ: {e}')
                sys.exit(1)
            finally:
                await client.disconnect()
                log('🔌 تم قطع الاتصال')
                
                # تنظيف الملفات
                log('🧹 تنظيف الملفات...')
                os.system("rm -f *.mp4 *.jpg 2>/dev/null || true")
                log('✅ تم التنظيف')
        
        if __name__ == "__main__":
            asyncio.run(main())
        EOF
        
        echo "📄 تم إنشاء سكربت الرفع"
        
    - name: تشغيل سكربت الرفع
      env:
        TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
        TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
        TELEGRAM_SESSION_STRING: ${{ secrets.TELEGRAM_SESSION_STRING }}
        CHAT_ID: ${{ github.event.inputs.chat_id }}
        CONTENT_NAME: ${{ github.event.inputs.content_name }}
        IMAGE_URL: ${{ github.event.inputs.image_url }}
        VIDEO_URL: ${{ github.event.inputs.video_url }}
      run: |
        echo "⚡ تشغيل سكربت الرفع..."
        python3 upload.py
        
    - name: عرض نتيجة الرفع
      run: |
        echo "========================================"
        echo "✅ تم تنفيذ سكربت الرفع"
        echo "🕒 الوقت: $(date)"
        echo "========================================"
