name: Telegram Personal Uploader

on:
  workflow_dispatch:
    inputs:
      chat_id:
        description: 'معرف القناة/الجروب'
        required: true
        type: string
      content_name:
        description: 'اسم الفيلم/المسلسل'
        required: true
        type: string
      content_type:
        description: 'نوع المحتوى'
        required: true
        type: choice
        options:
        - movie
        - series
      image_url:
        description: 'رابط صورة الغلاف'
        required: true
        type: string
      video_url:
        description: 'رابط الفيديو'
        required: false
        type: string
      series_files:
        description: 'روابط الحلقات (مفصولة بفواصل)'
        required: false
        type: string

jobs:
  upload-to-telegram:
    runs-on: ubuntu-latest
    
    steps:
    - name: بدء العملية
      run: |
        echo "🚀 بدء عملية رفع المحتوى إلى Telegram"
        echo "📝 اسم المحتوى: ${{ github.event.inputs.content_name }}"
        echo "🎬 النوع: ${{ github.event.inputs.content_type }}"
        echo "⏰ وقت البدء: $(date)"
        
    - name: تثبيت المتطلبات
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          wget \
          ffmpeg \
          python3 \
          python3-pip \
          curl
        
        pip3 install telethon
        
    - name: إنشاء سكربت الرفع
      run: |
        cat > uploader.py << 'EOF'
        import asyncio
        import os
        import sys
        from telethon import TelegramClient
        from telethon.sessions import StringSession
        
        # بيانات من Secrets
        API_ID = int(os.environ.get('TELEGRAM_API_ID'))
        API_HASH = os.environ.get('TELEGRAM_API_HASH')
        SESSION_STRING = os.environ.get('TELEGRAM_SESSION_STRING')
        
        # بيانات من GitHub Actions
        CHAT_ID = os.environ.get('CHAT_ID')
        CONTENT_NAME = os.environ.get('CONTENT_NAME')
        CONTENT_TYPE = os.environ.get('CONTENT_TYPE')
        IMAGE_URL = os.environ.get('IMAGE_URL')
        VIDEO_URL = os.environ.get('VIDEO_URL', '')
        SERIES_FILES = os.environ.get('SERIES_FILES', '')
        
        async def main():
            print("=== بدء رفع المحتوى إلى Telegram ===")
            
            # إنشاء عميل Telegram
            client = TelegramClient(
                StringSession(SESSION_STRING),
                API_ID,
                API_HASH
            )
            
            try:
                # الاتصال
                await client.connect()
                print("✅ متصل بـ Telegram")
                
                # تنزيل ورفع الصورة
                print("📥 جاري تنزيل الصورة...")
                os.system(f'wget -O "poster.jpg" "{IMAGE_URL}"')
                
                print("📤 جاري رفع الصورة...")
                await client.send_file(
                    CHAT_ID,
                    'poster.jpg',
                    caption=f'🎬 {CONTENT_NAME}'
                )
                print("✅ تم رفع الصورة")
                
                if CONTENT_TYPE == 'movie' and VIDEO_URL:
                    # فيلم
                    print("📥 جاري تنزيل الفيديو...")
                    video_name = f"{CONTENT_NAME}.mp4"
                    os.system(f'wget -O "{video_name}" "{VIDEO_URL}"')
                    
                    print(f"📤 جاري رفع الفيديو: {video_name}")
                    await client.send_file(
                        CHAT_ID,
                        video_name,
                        caption=f'🎥 {CONTENT_NAME}'
                    )
                    print("✅ تم رفع الفيديو")
                    
                elif CONTENT_TYPE == 'series' and SERIES_FILES:
                    # مسلسل
                    files = SERIES_FILES.split(',')
                    for i, file_url in enumerate(files, 1):
                        if file_url.strip():
                            episode_name = f"{CONTENT_NAME} - الحلقة {i}.mp4"
                            print(f"📥 جاري تنزيل الحلقة {i}...")
                            os.system(f'wget -O "{episode_name}" "{file_url.strip()}"')
                            
                            print(f"📤 جاري رفع الحلقة {i}")
                            await client.send_file(
                                CHAT_ID,
                                episode_name,
                                caption=f'📺 {CONTENT_NAME} - الحلقة {i}'
                            )
                            await asyncio.sleep(1)
                    
                print("✅ تم اكتمال العملية بنجاح!")
                
            except Exception as e:
                print(f"❌ خطأ: {e}")
                sys.exit(1)
            finally:
                await client.disconnect()
                print("🔌 تم قطع الاتصال")
                
                # تنظيف الملفات
                print("🧹 جاري تنظيف الملفات المؤقتة...")
                os.system("rm -f *.mp4 *.jpg *.png")
                print("✅ تم التنظيف")
        
        if __name__ == "__main__":
            asyncio.run(main())
        EOF
        
        echo "📄 تم إنشاء سكربت الرفع"
        
    - name: تشغيل سكربت الرفع
      env:
        TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
        TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
        TELEGRAM_SESSION_STRING: ${{ secrets.TELEGRAM_SESSION_STRING }}
        CHAT_ID: ${{ github.event.inputs.chat_id }}
        CONTENT_NAME: ${{ github.event.inputs.content_name }}
        CONTENT_TYPE: ${{ github.event.inputs.content_type }}
        IMAGE_URL: ${{ github.event.inputs.image_url }}
        VIDEO_URL: ${{ github.event.inputs.video_url }}
        SERIES_FILES: ${{ github.event.inputs.series_files }}
      run: |
        echo "⚡ جاري تشغيل سكربت الرفع..."
        python3 uploader.py
        
    - name: نهاية العملية
      run: |
        echo "🏁 انتهت العملية في: $(date)"
        echo "✅ تم رفع ${{ github.event.inputs.content_name }} بنجاح"
