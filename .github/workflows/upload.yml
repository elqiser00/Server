name: 📤 رفع محتوى على تيليجرام - الإصدار النهائي

on:
  workflow_dispatch:
    inputs:
      mode:
        description: 'الوضع (اختر: movie للأفلام / series للمسلسلات)'
        required: true
        default: 'movie'
        type: choice
        options:
          - movie
          - series
      
      channel:
        description: 'رابط القناة: @yourchannel أو رابط دعوة (مثل +Abc123)'
        required: true
        type: string
      
      caption:
        description: 'الكابشن/الوصف (استخدم \\n للفصل بين الأسطر)'
        required: true
        type: string
      
      image_url:
        description: 'رابط مباشر للصورة (لوضع الأفلام فقط)'
        required: false
        type: string
      
      video_url:
        description: 'رابط مباشر للفيديو (لوضع الأفلام فقط)'
        required: false
        type: string
      
      video_name:
        description: 'اسم الفيديو (بدون .mp4)'
        required: false
        default: 'movie'
        type: string

jobs:
  upload:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    
    steps:
      - name: 🛠️ إعداد البيئة
        uses: actions/checkout@v4
        
      - name: 🐍 تثبيت بايثون 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: 🎬 تثبيت FFmpeg (مهم لاستخراج Thumbnail)
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg
          ffmpeg -version | head -1
          ffprobe -version | head -1
          echo "✅ FFmpeg و FFprobe تم التثبيت بنجاح"
      
      - name: 📦 تثبيت المكتبات
        run: |
          pip install --upgrade pip
          pip install telethon requests urllib3 pillow
      
      - name: 🔒 التحقق من الأسرار
        env:
          TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
          TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
          TELEGRAM_SESSION_STRING: ${{ secrets.TELEGRAM_SESSION_STRING }}
        run: |
          if [ -z "$TELEGRAM_API_ID" ] || [ -z "$TELEGRAM_API_HASH" ] || [ -z "$TELEGRAM_SESSION_STRING" ]; then
            echo "❌ المتغيرات السرية غير مكتملة"
            exit 1
          fi
          echo "✅ تم التحقق من الأسرار"
      
      - name: 🚀 تشغيل السكريبت
        env:
          MODE: ${{ github.event.inputs.mode }}
          CHANNEL: ${{ github.event.inputs.channel }}
          CAPTION: ${{ github.event.inputs.caption }}
          IMAGE_URL: ${{ github.event.inputs.image_url }}
          VIDEO_URL: ${{ github.event.inputs.video_url }}
          VIDEO_NAME: ${{ github.event.inputs.video_name }}
          TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
          TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
          TELEGRAM_SESSION_STRING: ${{ secrets.TELEGRAM_SESSION_STRING }}
        run: |
          python upload.py || exit 1
      
      - name: ✅ نجاح
        if: success()
        run: |
          echo "=========================================="
          echo "🎉 تم الرفع بنجاح!"
          echo "=========================================="
