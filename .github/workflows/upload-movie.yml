    - name: تشغيل سكربت الرفع
      env:
        TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
        TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
        TELEGRAM_SESSION_STRING: ${{ secrets.TELEGRAM_SESSION_STRING }}
        CHANNEL_LINK: ${{ github.event.inputs.channel_link }}
        MOVIE_NAME: ${{ github.event.inputs.movie_name }}
        POSTER_URL: ${{ github.event.inputs.poster_url }}
        VIDEO_URL: ${{ github.event.inputs.video_url }}
      run: |
        cat > upload.py << 'EOF'
        import asyncio, os
        from telethon import TelegramClient
        from telethon.sessions import StringSession
        
        async def main():
            # قراءة المتغيرات
            API_ID = int(os.environ['TELEGRAM_API_ID'])
            API_HASH = os.environ['TELEGRAM_API_HASH']
            SESSION = os.environ['TELEGRAM_SESSION_STRING']
            CHANNEL = os.environ['CHANNEL_LINK']
            MOVIE = os.environ['MOVIE_NAME']
            
            # تحميل الملفات
            os.system(f'curl -L --insecure --output poster.jpg "{os.environ["POSTER_URL"]}"')
            os.system(f'curl -L --insecure --output video.mp4 "{os.environ["VIDEO_URL"]}"')
            
            # الاتصال بالتليجرام
            client = TelegramClient(StringSession(SESSION), API_ID, API_HASH)
            await client.connect()
            
            if not await client.is_user_authorized():
                raise Exception("جلسة غير صالحة")
            
            channel = await client.get_entity(CHANNEL)
            
            # إرسال الصورة
            await client.send_file(channel, "poster.jpg", caption=f"🎬 {MOVIE}")
            
            # إرسال الفيديو
            await client.send_file(channel, "video.mp4", 
                                  caption=f"🎥 {MOVIE}\\n✅ فيلم كامل",
                                  supports_streaming=True)
            
            await client.disconnect()
            
            # تنظيف
            os.remove("poster.jpg")
            os.remove("video.mp4")
            
            print("✅ تم الرفع بنجاح!")
        
        asyncio.run(main())
        EOF
        
        python3 upload.py
