name: 🎬 رفع فيلم إلى تليجرام

on:
  workflow_dispatch:
    inputs:
      channel_link:
        description: 'رابط القناة'
        required: true
        type: string
      movie_name:
        description: 'اسم الفيلم'
        required: true
        type: string
      poster_url:
        description: 'رابط البوستر'
        required: true
        type: string
      video_url:
        description: 'رابط الفيديو'
        required: true
        type: string

jobs:
  upload:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
    - name: تثبيت المتطلبات
      run: |
        sudo apt update
        sudo apt install -y python3 python3-pip curl
        pip3 install telethon

    - name: تحميل ورفع الفيلم
      env:
        TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
        TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
        TELEGRAM_SESSION_STRING: ${{ secrets.TELEGRAM_SESSION_STRING }}
        CHANNEL_LINK: ${{ github.event.inputs.channel_link }}
        MOVIE_NAME: ${{ github.event.inputs.movie_name }}
        POSTER_URL: ${{ github.event.inputs.poster_url }}
        VIDEO_URL: ${{ github.event.inputs.video_url }}
      run: |
        echo "🚀 بدء رفع الفيلم: $MOVIE_NAME"
        echo "=================================="
        
        # تحميل الملفات
        echo "📥 تحميل الملفات..."
        curl -L --insecure --output poster.jpg "$POSTER_URL"
        curl -L --insecure --output video.mp4 "$VIDEO_URL"
        
        # التحقق من الملفات
        echo "📊 حجم الملفات:"
        ls -la poster.jpg video.mp4
        
        # تشغيل سكربت Python
        python3 -c "
import asyncio, os
from telethon import TelegramClient
from telethon.sessions import StringSession

print('🔗 الاتصال بالتليجرام...')

async def upload():
    client = TelegramClient(
        StringSession('$TELEGRAM_SESSION_STRING'),
        $TELEGRAM_API_ID,
        '$TELEGRAM_API_HASH'
    )
    
    await client.connect()
    
    if not await client.is_user_authorized():
        print('❌ جلسة غير صالحة')
        return
    
    me = await client.get_me()
    print(f'✅ متصل بـ: {me.first_name}')
    
    # الحصول على القناة
    channel = await client.get_entity('$CHANNEL_LINK')
    print(f'📢 القناة: {channel.title}')
    
    # إرسال الصورة
    print('🖼️ إرسال الصورة...')
    await client.send_file(
        channel,
        'poster.jpg',
        caption='🎬 $MOVIE_NAME',
        parse_mode='html'
    )
    
    # إرسال الفيديو
    print('🎥 إرسال الفيديو...')
    video_size = os.path.getsize('video.mp4') / (1024 * 1024)
    await client.send_file(
        channel,
        'video.mp4',
        caption=f'🎥 $MOVIE_NAME\\n✅ فيلم كامل\\n📊 الحجم: {video_size:.2f} MB',
        parse_mode='html',
        supports_streaming=True
    )
    
    print('✅ تم الرفع بنجاح!')
    
    await client.disconnect()

asyncio.run(upload())
"
        
        # تنظيف
        rm -f poster.jpg video.mp4
        echo "🏁 انتهى التنفيذ"
