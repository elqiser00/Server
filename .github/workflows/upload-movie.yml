name: 🎬 رفع فيلم - الإصدار النهائي

on:
  workflow_dispatch:
    inputs:
      channel_link:
        description: 'رابط القناة'
        required: true
        type: string
      movie_name:
        description: 'اسم الفيلم'
        required: true
        type: string
      poster_url:
        description: 'رابط البوستر'
        required: true
        type: string
      video_url:
        description: 'رابط الفيديو المباشر mp4'
        required: true
        type: string

jobs:
  upload:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
    - name: تثبيت المتطلبات
      run: |
        echo "📦 تثبيت المتطلبات..."
        sudo apt update
        sudo apt install -y python3 python3-pip curl ffmpeg
        pip3 install telethon

    - name: إنشاء سكربت الرفع
      run: |
        echo "📝 إنشاء سكربت الرفع الجديد..."
        cat > upload.py << 'EOF'
        # **لصق الكود الجديد هنا (اختر أحد الكودين أعلاه)**
        EOF
        echo "✅ تم إنشاء upload.py"

    - name: تشغيل سكربت الرفع
      env:
        TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
        TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
        TELEGRAM_SESSION_STRING: ${{ secrets.TELEGRAM_SESSION_STRING }}
        CHANNEL_LINK: ${{ github.event.inputs.channel_link }}
        MOVIE_NAME: ${{ github.event.inputs.movie_name }}
        POSTER_URL: ${{ github.event.inputs.poster_url }}
        VIDEO_URL: ${{ github.event.inputs.video_url }}
      run: |
        echo "🔧 بدء الرفع..."
        python3 upload.py
