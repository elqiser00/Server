# .github/workflows/upload-movie-fixed.yml
name: 🎬 رفع فيلم - مصحح

on:
  workflow_dispatch:
    inputs:
      channel_link:
        description: 'رابط القناة'
        required: true
        default: 'https://t.me/+VvLRMffUCXNlNjRk'
        type: string
      movie_name:
        description: 'اسم الفيلم'
        required: true
        default: 'Truth & Treason 2025'
        type: string
      poster_url:
        description: 'رابط البوستر'
        required: true
        default: 'https://img.downet.net/uploads/U8xQf.webp'
        type: string
      video_url:
        description: 'رابط الفيديو'
        required: true
        type: string

jobs:
  upload:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    
    steps:
    - name: ⚙️ تثبيت المتطلبات
      run: |
        sudo apt-get update
        sudo apt-get install -y python3 python3-pip wget
        pip3 install telethon --quiet
        
    - name: 📝 إنشاء السكربت المصحح
      run: |
        # انسخ محتوى الكود أعلاه هنا
        cat > uploader_fixed.py << 'EOF'
        [ضع محتوى الكود Python الكامل هنا]
        EOF
        
    - name: ⚡ تشغيل الرفع
      env:
        TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
        TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
        TELEGRAM_SESSION_STRING: ${{ secrets.TELEGRAM_SESSION_STRING }}
        CHANNEL_LINK: ${{ github.event.inputs.channel_link }}
        MOVIE_NAME: ${{ github.event.inputs.movie_name }}
        POSTER_URL: ${{ github.event.inputs.poster_url }}
        VIDEO_URL: ${{ github.event.inputs.video_url }}
      run: |
        echo "⚡ بدء رفع فيلم (مصحح)..."
        echo "========================================"
        python3 uploader_fixed.py
        echo "========================================"
