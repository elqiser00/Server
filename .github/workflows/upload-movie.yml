    - name: رفع الفيلم إلى تليجرام
      env:
        TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
        TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
        TELEGRAM_SESSION_STRING: ${{ secrets.TELEGRAM_SESSION_STRING }}
        CHANNEL_LINK: ${{ github.event.inputs.channel_link }}
        MOVIE_NAME: ${{ github.event.inputs.movie_name }}
        POSTER_URL: ${{ github.event.inputs.poster_url }}
        VIDEO_URL: ${{ github.event.inputs.video_url }}
      run: |
        # إنشاء ملف Python
        cat > upload_movie.py << 'EOF'
        import asyncio, os, sys
        
        print("=" * 60)
        print("🚀 بدء رفع الفيلم إلى تليجرام")
        print("=" * 60)
        
        # قراءة المتغيرات
        API_ID = int(os.environ['TELEGRAM_API_ID'])
        API_HASH = os.environ['TELEGRAM_API_HASH']
        SESSION = os.environ['TELEGRAM_SESSION_STRING']
        CHANNEL = os.environ['CHANNEL_LINK']
        MOVIE = os.environ['MOVIE_NAME']
        POSTER_URL = os.environ['POSTER_URL']
        VIDEO_URL = os.environ['VIDEO_URL']
        
        print(f"🎬 الفيلم: {MOVIE}")
        print(f"📢 القناة: {CHANNEL}")
        
        from telethon import TelegramClient
        from telethon.sessions import StringSession
        
        async def upload_to_telegram():
            try:
                # تحميل الملفات
                print("\n📥 جاري تحميل الملفات...")
                os.system(f'curl -L --insecure --max-time 300 --output poster.jpg "{POSTER_URL}"')
                os.system(f'curl -L --insecure --max-time 600 --output video.mp4 "{VIDEO_URL}"')
                
                if not os.path.exists("poster.jpg"):
                    raise Exception("❌ فشل تحميل الصورة")
                if not os.path.exists("video.mp4"):
                    raise Exception("❌ فشل تحميل الفيديو")
                
                poster_size = os.path.getsize("poster.jpg") / 1024
                video_size = os.path.getsize("video.mp4") / (1024 * 1024)
                print(f"✅ الصورة: {poster_size:.1f} KB")
                print(f"✅ الفيديو: {video_size:.2f} MB")
                
                # الاتصال بالتليجرام
                print("\n🔗 جاري الاتصال بالتليجرام...")
                client = TelegramClient(StringSession(SESSION), API_ID, API_HASH)
                await client.connect()
                
                if not await client.is_user_authorized():
                    raise Exception("❌ جلسة التليجرام غير صالحة")
                
                me = await client.get_me()
                print(f"✅ تم الاتصال بـ: {me.first_name} (@{me.username})")
                
                # الحصول على القناة
                print(f"\n🔍 جاري البحث عن القناة...")
                channel = await client.get_entity(CHANNEL)
                print(f"📢 تم العثور على: {channel.title}")
                
                # إرسال الصورة
                print("\n🖼️ جاري إرسال الصورة...")
                await client.send_file(
                    channel,
                    "poster.jpg",
                    caption=f"🎬 {MOVIE}",
                    parse_mode='html'
                )
                print("✅ تم إرسال الصورة")
                
                # إرسال الفيديو
                print("\n🎥 جاري إرسال الفيديو (قد يستغرق دقائق)...")
                await client.send_file(
                    channel,
                    "video.mp4",
                    caption=f"🎥 {MOVIE}\n✅ فيلم كامل\n📊 الحجم: {video_size:.2f} MB",
                    parse_mode='html',
                    supports_streaming=True
                )
                print("✅ تم إرسال الفيديو")
                
                # تنظيف
                os.remove("poster.jpg")
                os.remove("video.mp4")
                
                print("\n" + "=" * 60)
                print("🎉🎉🎉 تم رفع الفيلم بنجاح! 🎉🎉🎉")
                print("=" * 60)
                
                await client.disconnect()
                
            except Exception as e:
                print(f"\n❌❌❌ خطأ: {e}")
                sys.exit(1)
        
        # تشغيل الدالة
        asyncio.run(upload_to_telegram())
        EOF
        
        # تشغيل السكربت
        python3 upload_movie.py
