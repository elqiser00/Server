name: Upload Media to Telegram

on:
  workflow_dispatch:
    inputs:
      channel_url:
        description: 'رابط القناة (مثال: @channel أو https://t.me/channel أو +123456789)'
        required: true
        type: string
      media_type:
        description: 'نوع المحتوى'
        required: true
        default: 'أفلام'
        type: choice
        options:
          - أفلام
          - مسلسلات
      logo_url:
        description: 'رابط صورة الشعار (اختياري)'
        required: false
        type: string
      video_paths:
        description: 'روابط الفيديو (مفصولة بفواصل)'
        required: true
        type: string
      caption:
        description: 'النص التوضيحي (اختياري)'
        required: false
        type: string

jobs:
  upload:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 استعراض الكود
      uses: actions/checkout@v3
      
    - name: 🐍 إعداد Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: 📦 تثبيت المتطلبات
      run: |
        pip install telethon==1.34.0
        pip install aiohttp==3.9.1
        
    - name: 🚀 رفع الملفات
      env:
        # الأسرار
        TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
        TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
        TELEGRAM_PHONE: ${{ secrets.TELEGRAM_PHONE }}
        TELEGRAM_PASSWORD: ${{ secrets.TELEGRAM_PASSWORD }}
        TELEGRAM_SESSION_STRING: ${{ secrets.TELEGRAM_SESSION_STRING }}
        
        # المدخلات
        INPUT_CHANNEL_URL: ${{ inputs.channel_url }}
        INPUT_MEDIA_TYPE: ${{ inputs.media_type }}
        INPUT_LOGO_URL: ${{ inputs.logo_url }}
        INPUT_VIDEO_PATHS: ${{ inputs.video_paths }}
        INPUT_CAPTION: ${{ inputs.caption }}
      run: |
        echo "========================================"
        echo "🚀 بدء رفع الملفات إلى التليجرام"
        echo "========================================"
        echo "القناة: ${{ inputs.channel_url }}"
        echo "النوع: ${{ inputs.media_type }}"
        echo ""
        python main.py
        
    - name: 📊 النتيجة
      if: always()
      run: |
        echo "========================================"
        if [ "${{ job.status }}" = "success" ]; then
          echo "✅ تم بنجاح!"
        else
          echo "❌ فشلت العملية"
          echo ""
          echo "🔍 الأسباب المحتملة:"
          echo "1. تأكد من صحة الأسرار في GitHub Secrets"
          echo "2. تحقق من رابط القناة"
          echo "3. تأكد أن روابط الفيديو نشطة"
          echo "4. تحقق من تفعيل حساب التليجرام"
        fi
        echo "========================================"
