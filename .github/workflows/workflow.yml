name: Upload Media to Telegram

on:
  workflow_dispatch:
    inputs:
      channel_url:
        description: 'رابط القناة على التليجرام (مثال: @channel_name أو https://t.me/channel_name)'
        required: true
        type: string
      media_type:
        description: 'نوع المحتوى المراد رفعه'
        required: true
        default: 'أفلام'
        type: choice
        options:
          - أفلام
          - مسلسلات
      logo_url:
        description: 'رابط الشعار (لوغو) - اختياري'
        required: false
        type: string
      video_paths:
        description: 'مسارات ملفات الفيديو (مفصولة بفواصل) - مثال: /path/to/video1.mp4,/path/to/video2.mp4'
        required: true
        type: string
      caption:
        description: 'النص التوضيحي (الكبشر) - اختياري'
        required: false
        type: string

jobs:
  upload:
    runs-on: ubuntu-latest
    
    steps:
    - name: التحقق من المستودع
      uses: actions/checkout@v3
      
    - name: إعداد Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: تثبيت المتطلبات
      run: |
        python -m pip install --upgrade pip
        pip install telethon==1.34.0
        pip install aiohttp==3.9.1
        
    - name: رفع الملفات إلى التليجرام
      env:
        TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
        TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
        TELEGRAM_PHONE: ${{ secrets.TELEGRAM_PHONE }}
        TELEGRAM_PASSWORD: ${{ secrets.TELEGRAM_PASSWORD }}
        TELEGRAM_SESSION_STRING: ${{ secrets.TELEGRAM_SESSION_STRING }}
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        INPUT_CHANNEL_URL: ${{ inputs.channel_url }}
        INPUT_MEDIA_TYPE: ${{ inputs.media_type }}
        INPUT_LOGO_URL: ${{ inputs.logo_url }}
        INPUT_VIDEO_PATHS: ${{ inputs.video_paths }}
        INPUT_CAPTION: ${{ inputs.caption }}
      run: |
        echo "🎬 بدء عملية الرفع..."
        echo "نوع المحتوى: ${{ inputs.media_type }}"
        echo "رابط القناة: ${{ inputs.channel_url }}"
        python upload_media.py
        
    - name: إرسال نتيجة الرفع
      if: always()
      run: |
        if [ "${{ job.status }}" = "success" ]; then
          echo "✅ تم رفع الملفات بنجاح!"
        else
          echo "❌ فشل رفع الملفات"
        fi
