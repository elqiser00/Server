name: Upload Media to Telegram

on:
  workflow_dispatch:
    inputs:
      channel_url:
        description: 'رابط القناة (مثال: @channel أو +123456789 أو https://t.me/channel)'
        required: true
        type: string
      media_type:
        description: 'نوع المحتوى'
        required: true
        default: 'أفلام'
        type: choice
        options:
          - أفلام
          - مسلسلات
      logo_url:
        description: 'رابط صورة الشعار (اختياري)'
        required: false
        type: string
      video_paths:
        description: 'روابط الفيديو (مفصولة بفواصل)'
        required: true
        type: string
      caption:
        description: 'النص التوضيحي (اختياري)'
        required: false
        type: string

env:
  # المتغيرات الثابتة
  PYTHONUNBUFFERED: "1"

jobs:
  upload:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 استعراض الكود
      uses: actions/checkout@v3
      
    - name: 🐍 إعداد Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: 📦 تثبيت المتطلبات
      run: |
        pip install --upgrade pip
        pip install telethon==1.34.0
        pip install aiohttp==3.9.1
        pip install python-dotenv==1.0.0
        
    - name: 🗂️ إنشاء مجلدات
      run: |
        mkdir -p sessions downloads
        
    - name: 🚀 رفع الملفات
      env:
        # الأسرار المطلوبة
        TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
        TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
        TELEGRAM_PHONE: ${{ secrets.TELEGRAM_PHONE }}
        TELEGRAM_PASSWORD: ${{ secrets.TELEGRAM_PASSWORD || '' }}
        TELEGRAM_SESSION_STRING: ${{ secrets.TELEGRAM_SESSION_STRING || '' }}
        
        # مدخلات المستخدم
        INPUT_CHANNEL_URL: ${{ inputs.channel_url }}
        INPUT_MEDIA_TYPE: ${{ inputs.media_type }}
        INPUT_LOGO_URL: ${{ inputs.logo_url }}
        INPUT_VIDEO_PATHS: ${{ inputs.video_paths }}
        INPUT_CAPTION: ${{ inputs.caption }}
      run: |
        echo "========================================"
        echo "🚀 بدء رفع الملفات إلى التليجرام"
        echo "========================================"
        python main.py
        
    - name: 📊 نتيجة الرفع
      if: always()
      run: |
        echo "========================================"
        if [ "${{ job.status }}" = "success" ]; then
          echo "✅ تم بنجاح!"
        else
          echo "❌ فشلت العملية"
          echo ""
          echo "🔍 استكشاف الأخطاء:"
          echo "1. تحقق من ملف uploader.log للتفاصيل"
          echo "2. تأكد من صحة الأسرار في GitHub Secrets"
          echo "3. تأكد من أن الحساب مفعل وله صلاحية النشر"
          echo "4. جرب إعداد الجلسة محلياً أولاً"
        fi
        echo "========================================"
        
    - name: 📁 حفظ السجلات
      if: always()
      uses: actions/upload-artifact@v3
      with:
        name: upload-logs
        path: |
          uploader.log
          sessions/
        retention-days: 7
